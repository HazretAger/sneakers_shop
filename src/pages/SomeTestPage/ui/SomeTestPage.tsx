import { Wrapper } from '@/shared/ui/Wrapper';
import { AllFieldForm } from '@/features/TestFeatures';

const SomeTestPage = () => (
    <Wrapper>
        <AllFieldForm />
    </Wrapper>
);

export default SomeTestPage;
